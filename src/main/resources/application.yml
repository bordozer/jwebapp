spring:
  application:
    name: webapp
  profiles:
    active: dev

server:
  port: 9011
  servlet:
    session:
      timeout: 864000

logging:
  level:
    root: info

management:
  server:
    port: 9111
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env,heapdump,sessions,httptrace,logfile

---
spring:
  profiles: dev

---
spring:
  profiles: aws-test

---
spring:
  profiles: aws-prod
